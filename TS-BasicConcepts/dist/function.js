"use strict";
{
    function add(a, b) {
        return a + b;
    }
    add(1, 2);
    let add2 = (a, b) => a + b;
    add2(1, 2);
    let add3 = (a, b) => a + b;
    add3(1, 2);
    let add4 = (a, b) => a + b;
    add4(1, 2);
}
